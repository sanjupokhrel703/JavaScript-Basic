// Arithmetic operators
// Sum +
const sum = 5 + 7;
console.log(sum);

// Sub -
const sub = 96 - 53;
console.log(sub);

// mul *
const mul = 8 * 56;
console.log(mul);

// div /
const div = 98 / 5;
console.log(div);

// remainder %
const rem = 98 % 5;
console.log(rem);

// increment ++ => increases value by 1;
let num = 5;
num++;

console.log(num);

// decrement -- => decreases value by 1
num--;
console.log(num);

// Logical operators
// Equality operator ==
let x = 5;
x = 7;
x == 6;

// AND && : If one of the value is false, then result is false;
const andOpr = x == 6 && false;
console.log(first);

// OR || : If one of the value is true, then result is true;
const orOpr = x == 6 || true;

// NOT ! : if value is true, result is false;
x != 6;

